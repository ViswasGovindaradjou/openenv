[project]
name = "openenv-support"
version = "0.1.0"
description = "OpenEnv SLA Support Environment"
requires-python = ">=3.10"

dependencies = [
    "fastapi==0.111.0",
    "uvicorn==0.30.0",
    "pydantic==1.10.15",
    "python-dotenv==1.0.1",
    "requests"
]
